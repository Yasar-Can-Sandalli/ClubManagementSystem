<UserControl x:Class="ClubManagementSystem.Views.EventsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             xmlns:iconPacks="clr-namespace:MahApps.Metro.IconPacks;assembly=MahApps.Metro.IconPacks.Material"
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid Background="#F0F7F7">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="40,30,40,30">
            <TextBlock Text="Kulüp Etkinlikleri" Foreground="#1D333F" FontSize="28" FontWeight="Bold" VerticalAlignment="Center"/>
            
            <Button HorizontalAlignment="Right" Background="#00A8FF" Foreground="White" Padding="20,10" BorderThickness="0" Cursor="Hand" Click="BtnAddEvent_Click">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="CalendarPlus" Width="15" Height="15" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Yeni Etkinlik Planla" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="10"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>

        <Border Grid.Row="1" Background="White" CornerRadius="18" Margin="40,0,40,40" Padding="15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="25" ShadowDepth="1" Color="Gray" Opacity="0.1"/>
            </Border.Effect>
            <DataGrid x:Name="EventsDataGrid" AutoGenerateColumns="False"
                      Background="White" RowBackground="White" AlternatingRowBackground="#F8FAFC"
                      Foreground="#333333" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#E2E8F0" 
                      BorderThickness="0" HeadersVisibility="Column" CanUserAddRows="False" RowHeight="55" FontSize="14">
                
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="Foreground" Value="#78909C"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="10,0,0,15"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#E2E8F0"/>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Tarih" Binding="{Binding EventDate, StringFormat='dd.MM.yyyy HH:mm'}" Width="150"/>
                    <DataGridTextColumn Header="Etkinlik Adı" Binding="{Binding EventName}" Width="2*"/>
                    <DataGridTextColumn Header="Konum / Platform" Binding="{Binding Location}" Width="1.5*"/>
                    <DataGridTextColumn Header="Açıklama" Binding="{Binding Description}" Width="3*"/>
                    
                    <DataGridTemplateColumn Header="İşlemler" Width="110">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Click="BtnEdit_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,0,12,0" ToolTip="Düzenle">
                                        <iconPacks:PackIconMaterial Kind="PencilOutline" Foreground="#623ED0" Width="18" Height="18"/>
                                    </Button>
                                    <Button Click="BtnDelete_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" ToolTip="Sil">
                                        <iconPacks:PackIconMaterial Kind="DeleteOutline" Foreground="#E53935" Width="18" Height="18"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>