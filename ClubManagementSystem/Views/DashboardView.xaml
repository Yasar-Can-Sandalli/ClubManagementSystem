<UserControl x:Class="ClubManagementSystem.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="clr-namespace:MahApps.Metro.IconPacks;assembly=MahApps.Metro.IconPacks.Material"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="720" d:DesignWidth="1000">

    <Grid Background="#F0F7F7"> 
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="40,35,40,25">
            <StackPanel>
                <TextBlock Text="Yönetim Paneli" Foreground="#1D333F" FontSize="30" FontWeight="Bold"/>
                <TextBlock Text="ROTOM DX Kulübü gerçek zamanlı sistem analitiği ve program akışı." Foreground="#78909C" FontSize="14" Margin="0,4,0,0"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="40,0,40,40">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.3*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="0,0,25,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="White" CornerRadius="18" Padding="20" Margin="0,0,0,25">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="25" ShadowDepth="1" Color="#1D333F" Opacity="0.06"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="Kulüp Takvimi ve Program" Foreground="#1D333F" FontSize="16" FontWeight="Bold" Margin="5,0,0,15"/>
                        <Calendar x:Name="ClubCalendar" Background="White" BorderThickness="0" HorizontalAlignment="Stretch" FontSize="14"/>
                    </StackPanel>
                </Border>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="White" CornerRadius="18" Padding="20" Margin="0,0,12,0">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="20" ShadowDepth="1" Color="#1D333F" Opacity="0.05"/>
                        </Border.Effect>
                        <StackPanel VerticalAlignment="Center">
                            <iconPacks:PackIconMaterial Kind="AccountGroup" Foreground="#623ED0" Width="28" Height="28"/>
                            <TextBlock Text="Kayıtlı Toplam Üye" Foreground="#78909C" FontSize="13" Margin="0,12,0,2" FontWeight="SemiBold"/>
                            <TextBlock x:Name="TxtTotalMembers" Text="0" Foreground="#1D333F" FontSize="36" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Background="White" CornerRadius="18" Padding="20" Margin="12,0,0,0">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="20" ShadowDepth="1" Color="#1D333F" Opacity="0.05"/>
                        </Border.Effect>
                        <StackPanel VerticalAlignment="Center">
                            <iconPacks:PackIconMaterial Kind="CalendarCheck" Foreground="#00A8FF" Width="28" Height="28"/>
                            <TextBlock Text="Aktif Etkinlikler" Foreground="#78909C" FontSize="13" Margin="0,12,0,2" FontWeight="SemiBold"/>
                            <TextBlock x:Name="TxtTotalEvents" Text="0" Foreground="#1D333F" FontSize="36" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>

            <Border Grid.Column="1" Background="White" CornerRadius="18" Padding="25">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="25" ShadowDepth="1" Color="#1D333F" Opacity="0.06"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Yaklaşan Etkinlik Akışı" Foreground="#1D333F" FontSize="16" FontWeight="Bold" Margin="0,0,0,20"/>

                    <ListBox x:Name="LstUpcomingEvents" Grid.Row="1" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#F8FAFC" CornerRadius="12" Padding="15" Margin="0,0,0,12" BorderBrush="#E2E8F0" BorderThickness="1">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <Border Grid.Column="0" Width="40" Height="40" CornerRadius="20" Background="#EDE7F6" Margin="0,0,12,0">
                                            <iconPacks:PackIconMaterial Kind="ClockOutline" Foreground="#623ED0" Width="18" Height="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>

                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding EventName}" Foreground="#1D333F" FontSize="14" FontWeight="Bold"/>
                                            <TextBlock Text="{Binding Location}" Foreground="#78909C" FontSize="12" Margin="0,2,0,0"/>
                                        </StackPanel>

                                        <Border Grid.Column="2" Background="#623ED0" CornerRadius="6" Padding="8,4" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding DaysRemaining}" Foreground="White" FontSize="11" FontWeight="Bold"/>
                                        </Border>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <ContentPresenter />
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>